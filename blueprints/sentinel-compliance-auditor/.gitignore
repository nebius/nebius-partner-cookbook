/.env
/.venv
.langgraph_api/
__pycache__/
output/
.claude/
*.pyc
*.egg-info/
.DS_Store
.idea/
.mcp.json
.codex/

# Blueprint installs via pip (Makefile), not uv — ignore stray uv lockfile
uv.lock

# Copyrighted regulation texts — local only, never commit (AICPA / PCI SSC)
data/regulations/soc2_*
data/regulations/pci_dss_quick_guide.*

# Bulky per-repeat eval files — keep locally, commit only the variance_* summaries
data/eval/results/*_r[2-9]_*.json

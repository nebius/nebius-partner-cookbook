# AGENTS.md — Nebius Cookbook

This file is the **single source of truth** for any coding agent working on this repository. Read it in full before making changes. If something here contradicts what you find in the code, this file wins until updated.

---

## 1. Project at a glance

The Nebius Cookbook is a public, GitHub-hosted collection of **production-grade recipes** for building AI agents on [Nebius AgentKit](https://nebius.com/services/token-factory). It launches at the Nebius customer conference on **June 9, 2026** (press and analysts in the room) together with a live keynote demo.

**Ship deadline: ten cookbooks must be ready by June 4, 2026** (five working days before the keynote, to leave room for rehearsal, review, and last-minute fixes). The arc now runs from a first production-shaped agent, through retrieval and real-time context, then LangChain/LangGraph orchestration, short-term memory, long-term memory, LangSmith observability, guardrails, MCP-powered actions, and simulation.

It is modeled on the best of:
- `developers.openai.com/cookbook` for pedagogical recipes
- `github.com/vercel/next.js/tree/canary/examples` for runnable starters

The two deliverables are:

1. **Cookbook recipes** — Python FastAPI apps, each a runnable production-shaped starter, each demonstrating one specific Nebius integration.
2. **Cookbook web app** — Next.js site that renders the recipes catalog with rich metadata, hosted on Clever Cloud.

The repo is a **monorepo** with both worlds living together: JavaScript (the app, shared packages, build scripts) and Python (the cookbook recipes themselves).

---

## 2. Hard constraints

These are non-negotiable. If a change would violate any of them, stop and ask.

- **Production-shaped, not toy-shaped.** Every recipe must look like code that could land in a real codebase. Type hints, error handling, observability hooks, sensible logging, healthchecks, Dockerfile. No `print()` debugging, no hardcoded credentials, no `except Exception: pass`.
- **Runnable in 5 minutes.** Any recipe that requires more than five minutes to clone, configure, and run on a fresh laptop fails the cookbook standard.
- **Each cookbook is autonomous.** Cloning a single cookbook directory must be sufficient to run it. No imports from sibling cookbooks. No shared Python packages. Duplication of skeleton code between cookbooks is accepted and expected.
- **Story before features.** Every recipe earns its place by demonstrating one concrete piece of the prototype → production journey for AI agents. If a feature is technically interesting but does not advance the story, cut it.
- **Boring infrastructure choices.** The stack is already opinionated. Everything else (HTTP framework, package manager, logging library, CI) defaults to the boringly conventional choice. No demonstrating personal taste in tooling.
- **No code that calls into networks during tests by default.** Use `respx` to mock Nebius and external APIs. Tests must pass with no network access.
- **Secrets never in code.** Always env vars, validated through Pydantic Settings at boot. `.env.example` lists every required key.

---

## 3. Repository layout

```
nebius-cookbook/
├── README.md                          # Generated by `bun run build:readme`
├── LICENSE                            # MIT
├── CONTRIBUTING.md                    # Hand-edited, short, points to README/04
├── AGENTS.md                          # This file
├── .gitignore
├── .editorconfig
├── .nvmrc                             # Node version (22.x)
├── .python-version                    # Python 3.12
├── bunfig.toml
├── package.json                       # Workspaces root
├── bun.lockb                          # Committed
│
├── README/                            # Sources composed into README.md
│   ├── _config.json                   # Composition manifest
│   ├── 01-intro.md                    # Human
│   ├── 03-getting-started.md          # Human
│   ├── 04-contributing.md             # Human
│   ├── 05-stack.md                    # Human
│   └── 99-license-credits.md          # Human
│
├── schemas/
│   └── recipe.schema.json             # Source of truth for recipe metadata
│
├── cookbooks/                         # Python recipes (uv-managed per cookbook)
│   ├── 01-first-agent-on-nebius/
│   │   ├── recipe.json
│   │   ├── README.md
│   │   ├── pyproject.toml
│   │   ├── uv.lock
│   │   ├── .python-version
│   │   ├── .env.example
│   │   ├── Dockerfile
│   │   ├── Makefile
│   │   ├── app/
│   │   │   ├── __init__.py
│   │   │   ├── main.py
│   │   │   ├── config.py
│   │   │   ├── routes/
│   │   │   ├── schemas/
│   │   │   ├── core/
│   │   │   └── observability/
│   │   ├── tests/
│   │   ├── docs/
│   │   └── assets/
│   ├── 02-real-world-data-nebius-tavily/
│   │   └── (same structure)
│   └── _template/                     # Bootstrap source for new cookbooks
│
├── app/                               # Next.js, deployed on Clever Cloud
│   ├── package.json
│   ├── next.config.mjs
│   ├── tsconfig.json
│   ├── tailwind.config.ts
│   ├── postcss.config.mjs
│   ├── .env.example
│   ├── public/
│   └── src/
│       ├── app/
│       ├── components/
│       ├── content/                   # Generated at build (gitignored)
│       └── lib/
│
├── packages/
│   ├── recipe-schema/                 # JSON Schema + generated TS types
│   └── mdx-pipeline/                  # Cookbook README → MDX compiler
│
├── scripts/                           # TypeScript, executed by Bun
│   ├── build-readme.ts                # Compose README.md from README/
│   ├── generators/
│   │   └── recipes-table.ts
│   ├── validate-recipes.ts
│   ├── build-recipes.ts               # Cookbook READMEs → MDX for app
│   ├── new-cookbook.ts                # Bootstrap from _template
│   ├── test-cookbooks.ts              # Run pytest on every cookbook
│   └── check-links.ts
│
├── docs/
│   ├── architecture.md
│   ├── adding-a-cookbook.md
│   └── deployment.md
│
└── .github/workflows/
    ├── test-cookbooks.yml
    ├── build-app.yml
    ├── validate-recipes.yml
    └── deploy-app.yml
```

---

## 4. Package management

### 4.1 JavaScript / TypeScript: Bun

Bun manages the JS workspace at the root. Workspaces declared in `package.json` cover:
- `app/`
- `packages/*`

**Bun is NOT declared as a workspace manager for `cookbooks/`.** Those are Python projects, intentionally isolated from the JS world.

Use:
- `bun install` to install JS deps for the whole workspace
- `bun run <script>` to execute scripts declared in `package.json`
- Bun executes TypeScript directly. No transpile step. Files in `scripts/` are `.ts` and run via `bun run scripts/foo.ts`.

**Runtime:** Bun is used in dev and CI. **Production runtime on Clever Cloud is Node** (LTS). The `next build` output is a standard Node.js Next.js app. This means: nothing in `app/src/` should depend on Bun-specific APIs at runtime.

### 4.2 Python: uv per cookbook

Each cookbook is an independent uv project with its own `pyproject.toml` and `uv.lock`. **There is no root-level Python workspace.** This is intentional.

Use:
- `uv sync --frozen` inside a cookbook to install deps from the lockfile
- `uv run <command>` to execute commands in the cookbook's virtualenv
- `uv add <package>` to add a dep
- `uv lock` to refresh the lockfile

A contributor who only touches Python does not need Bun. A contributor who only touches the app does not need uv.

### 4.3 Version pins

- Node: `22.x` (in `.nvmrc`)
- Python: `3.12` (in `.python-version` at root and per cookbook)
- Bun: `>= 1.1` (declared in `package.json` via `"packageManager": "bun@1.1.x"`)

---

## 5. The `recipe.json` contract

Every cookbook MUST have a `recipe.json` validated against `schemas/recipe.schema.json`. This file is the source of truth for:
- The recipes table in the root README
- The cookbook web app catalog
- OpenGraph images and SEO metadata
- The auto-generated cookbook page on the site

### 5.1 Required fields

```json
{
  "$schema": "../../schemas/recipe.schema.json",
  "slug": "first-agent-on-nebius",
  "order": 1,
  "title": "Your First Agent on Nebius",
  "tagline": "A production-grade FastAPI agent with streaming, observability, and zero magic.",
  "difficulty": "beginner",
  "estimatedReadingTime": "8 min",
  "estimatedRunTime": "5 min",
  "stack": {
    "primary": ["nebius-agentkit"],
    "secondary": ["fastapi", "structlog", "prometheus"]
  },
  "tags": ["agent", "streaming", "observability", "fastapi"],
  "story": {
    "problem": "...",
    "solution": "...",
    "outcome": "..."
  },
  "prerequisites": ["Python 3.12+", "A Nebius API key", "Docker (optional)"],
  "models": [{ "id": "meta-llama/Llama-3.3-70B-Instruct", "role": "primary" }],
  "assets": {
    "hero": "assets/hero.png",
    "demo": "assets/demo.gif",
    "architecture": "docs/architecture.svg"
  },
  "quickstart": {
    "clone": "git clone ... && cd cookbooks/01-first-agent-on-nebius",
    "configure": "cp .env.example .env  # then fill NEBIUS_API_KEY",
    "run": "make dev"
  },
  "endpoints": [
    { "method": "POST", "path": "/agent/run", "streaming": true, "description": "..." }
  ],
  "deployment": { "target": "nebius-compute", "instructions": "docs/deployment.md" },
  "nextRecipe": "real-world-data-nebius-tavily",
  "authors": [{ "name": "Clément Sauvage", "url": "https://bitsncoffee.co" }],
  "publishedAt": "2026-06-09",
  "updatedAt": "2026-06-09"
}
```

### 5.2 Validation

- `bun run validate` validates every `recipe.json` in `cookbooks/*/`
- CI runs this on every PR
- A failing validation blocks merge

### 5.3 Adding a field

1. Update `schemas/recipe.schema.json`
2. Regenerate TS types: `cd packages/recipe-schema && bun run generate`
3. Update all existing `recipe.json` files
4. Document the field in `docs/adding-a-cookbook.md`

---

## 6. The root README composition

The root `README.md` is **NOT edited directly**. It is composed by `bun run build:readme` from sources in `README/`.

### 6.1 The pattern

`README/_config.json` declares an ordered list of sections. Each section is either:
- `"source": "human"` — points to a `.md` file in `README/`, included verbatim
- `"source": "generated"` — points to a generator in `scripts/generators/`, output included

The script writes the concatenated result to `README.md` at the repo root.

### 6.2 The `_config.json`

```json
{
  "$schema": "../schemas/readme-config.schema.json",
  "output": "../README.md",
  "separator": "\n\n",
  "header": {
    "comment": "This file is generated by `bun run build:readme`. Edit sources in README/ instead."
  },
  "sections": [
    { "id": "intro", "source": "human", "file": "01-intro.md" },
    {
      "id": "recipes",
      "source": "generated",
      "generator": "recipes-table",
      "options": {
        "cookbooksDir": "../cookbooks",
        "columns": ["order", "title", "stack", "difficulty", "readingTime"],
        "heading": "## Recipes"
      }
    },
    { "id": "stack", "source": "human", "file": "05-stack.md" },
    { "id": "getting-started", "source": "human", "file": "03-getting-started.md" },
    { "id": "contributing", "source": "human", "file": "04-contributing.md" },
    { "id": "license-credits", "source": "human", "file": "99-license-credits.md" }
  ]
}
```

### 6.3 The check mode

CI runs `bun run build:readme --check` which regenerates the README in memory and compares with the committed `README.md`. If they differ, CI fails. The contributor must run `bun run build:readme` locally and commit the result.

### 6.4 Cookbook READMEs are different

**Cookbook-level READMEs** (`cookbooks/*/README.md`) are hand-edited, single-file, NOT composed from sub-MD. The composition pattern applies only at the repo root level.

---

## 7. Cookbook structure and conventions

### 7.1 Directory naming

`cookbooks/NN-kebab-case-slug/` where `NN` is a zero-padded order number matching `recipe.json.order`. The slug must match `recipe.json.slug`.

Examples: `01-first-agent-on-nebius/`, `02-real-world-data-nebius-tavily/`.

### 7.2 Required files

Every cookbook directory MUST contain, at minimum:

- `recipe.json` — metadata, validated
- `README.md` — hand-edited, single file
- `pyproject.toml` — uv-managed
- `uv.lock` — committed lockfile
- `.python-version` — Python pin
- `.env.example` — every required env var, with placeholder values and comments
- `Dockerfile` — multi-stage, slim final image
- `Makefile` — at minimum: `dev`, `test`, `lint`, `format`, `docker`, `deploy`
- `app/` — FastAPI application code
- `tests/` — at minimum one happy-path test per route

### 7.3 The FastAPI app structure

```
app/
├── __init__.py
├── main.py                            # FastAPI app, lifespan, middleware wiring
├── config.py                          # Pydantic Settings, validated at boot
├── routes/
│   ├── __init__.py
│   ├── agent.py                       # POST /agent/run with SSE streaming
│   └── health.py                      # GET /healthz, GET /readyz
├── schemas/
│   ├── __init__.py
│   └── agent.py                       # Pydantic request/response models
├── core/
│   ├── __init__.py
│   ├── nebius_client.py               # Wrapper around OpenAI SDK pointed at Nebius
│   └── agent.py                       # The agent logic
└── observability/
    ├── __init__.py
    ├── logging.py                     # structlog setup, JSON format
    ├── metrics.py                     # Prometheus counters/histograms
    └── middleware.py                  # Request ID injection, latency tracking
```

---

## 8. Python stack (per cookbook)

### 8.1 Mandatory dependencies

```toml
# pyproject.toml [project.dependencies]
dependencies = [
    "fastapi >= 0.115",
    "uvicorn[standard] >= 0.32",
    "openai >= 1.50",               # SDK used with base_url pointed at Nebius
    "pydantic >= 2.9",
    "pydantic-settings >= 2.6",
    "structlog >= 24.4",
    "slowapi >= 0.1.9",
    "prometheus-client >= 0.21",
    "httpx >= 0.27",                # For outgoing HTTP, with timeouts
    "tenacity >= 9.0",              # For retry with exponential backoff
]
```

Cookbook #2 adds:
```toml
    "tavily-python >= 0.5",
```

### 8.2 Dev dependencies

```toml
[dependency-groups]
dev = [
    "pytest >= 8.3",
    "pytest-asyncio >= 0.24",
    "respx >= 0.22",
    "ruff >= 0.7",
]
```

### 8.3 Ruff configuration

In each cookbook's `pyproject.toml`:

```toml
[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "N", "ASYNC", "S", "RUF"]
ignore = ["S101"]  # asserts in tests

[tool.ruff.format]
quote-style = "double"
```

### 8.4 Nebius client wrapper pattern

`app/core/nebius_client.py` MUST:

- Use the OpenAI SDK with `base_url="https://api.studio.nebius.ai/v1/"` (verify the exact URL via Nebius docs at integration time)
- Read `NEBIUS_API_KEY` from Pydantic Settings, never directly from `os.environ`
- Configure explicit timeouts (`httpx.Timeout(connect=5, read=60, write=10, pool=5)`)
- Wrap calls in `tenacity` retry with exponential backoff for transient errors (429, 5xx)
- Return raw stream chunks for streaming endpoints — let the route handler format SSE events

### 8.5 Streaming SSE conventions

The agent endpoint emits SSE with **named events**, not raw text:

```
event: status
data: {"phase": "planning"}

event: token
data: {"text": "Snow"}

event: token
data: {"text": "flake"}

event: status
data: {"phase": "done"}

event: done
data: {}
```

Heartbeat every 15 seconds during long operations:
```
event: heartbeat
data: {}
```

On client disconnect (detected via `await request.is_disconnected()`), the route handler MUST cancel the upstream Nebius call.

### 8.6 Observability requirements

- **Logs are JSON.** structlog with a JSON renderer in production, console-friendly in dev (detected via `ENV` env var).
- **Every request gets a request ID.** Injected by middleware, returned in `X-Request-ID` response header, included in every log line.
- **Metrics on `/metrics`.** At minimum:
  - `http_requests_total{method, path, status}` counter
  - `http_request_duration_seconds{method, path}` histogram
  - `nebius_tokens_total{model, type=input|output}` counter
  - `nebius_request_duration_seconds{model}` histogram
- **No PII in logs.** If a prompt contains an email or phone, redact before logging.

### 8.7 Rate limiting

`slowapi` with sane defaults:
- 10 requests per minute per IP on `/agent/run`
- No limit on `/healthz`, `/readyz`, `/metrics`

### 8.8 CORS and security headers

In `main.py`:
- CORS allowlist read from settings, never `*` in production
- Security headers via middleware: `X-Content-Type-Options: nosniff`, `Strict-Transport-Security`, `Referrer-Policy: strict-origin-when-cross-origin`
- Request body size limit: 64 KB (enforced via Starlette)

### 8.9 Graceful shutdown

FastAPI lifespan handler:
- On startup: validate config, log version, register signal handlers
- On shutdown: close HTTP client, flush logs, drain in-flight requests with a 30-second timeout

---

## 9. JS/TS stack (the app)

### 9.1 Mandatory dependencies

```json
{
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@nebius-cookbook/recipe-schema": "workspace:*",
    "@nebius-cookbook/mdx-pipeline": "workspace:*",
    "next-mdx-remote": "^5.0.0",
    "tailwindcss": "^3.4.0",
    "@radix-ui/themes": "^3.0.0",
    "lucide-react": "^0.460.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0"
  }
}
```

### 9.2 TypeScript config

`tsconfig.json` strict mode, no `any`, target ES2022:

```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "ES2022"],
    "module": "esnext",
    "moduleResolution": "bundler"
  }
}
```

### 9.3 Recipes manifest

At build time, `scripts/build-recipes.ts` produces `app/src/content/recipes-manifest.json` and `app/src/content/recipes/<slug>.mdx` for each cookbook. The app imports the manifest at module load. **Never read filesystem at runtime.**

### 9.4 SSG everywhere

Every page that lists or displays recipes is statically generated. The app on Clever Cloud is essentially a static site with a tiny Node runtime for sitemap/feed generation. No database, no edge functions, no API routes that hit the cookbook content.

---

## 10. Scripts

All scripts live in `scripts/` and are TypeScript executed by Bun.

### 10.1 `build-readme.ts`

Composes the root `README.md` from `README/` sources following `README/_config.json`. Supports `--check` for CI mode.

### 10.2 `generators/recipes-table.ts`

Generator invoked by `build-readme.ts`. Reads every `cookbooks/*/recipe.json`, sorts by `order`, produces a Markdown table.

### 10.3 `validate-recipes.ts`

Validates every `recipe.json` against `schemas/recipe.schema.json` using Ajv. Reports all errors at once (don't fail on first).

### 10.4 `build-recipes.ts`

For each cookbook:
- Reads `recipe.json` and `README.md`
- Compiles README to MDX using `@nebius-cookbook/mdx-pipeline`
- Writes to `app/src/content/recipes/<slug>.mdx`
- Builds `app/src/content/recipes-manifest.json`

### 10.5 `new-cookbook.ts`

Interactive prompt: title, slug, partner, difficulty. Copies `cookbooks/_template/` to `cookbooks/NN-<slug>/`, replaces placeholders, prints next-steps instructions.

### 10.6 `test-cookbooks.ts`

For each `cookbooks/*/`:
- `cd` into it
- Run `uv sync --frozen`
- Run `uv run pytest`
- Run `uv run ruff check`
- Aggregate exit codes; fail if any cookbook fails

### 10.7 `check-links.ts`

Walks all Markdown files in the repo, extracts links, verifies internal links resolve and external links return 2xx (with caching to avoid hammering external sites).

---

## 11. CI workflows

### 11.1 `validate-recipes.yml`

On push and PR. Steps:
- `bun install`
- `bun run validate`
- `bun run build:readme --check`

### 11.2 `test-cookbooks.yml`

On push and PR. Matrix over cookbooks. For each:
- Setup Python 3.12 and uv
- `cd cookbooks/<name>`
- `uv sync --frozen`
- `uv run pytest`
- `uv run ruff check`
- `uv run ruff format --check`

### 11.3 `build-app.yml`

On push and PR. Steps:
- `bun install`
- `bun run build:recipes`
- `cd app && bun run build`

### 11.4 `deploy-app.yml`

On push to `main` only. Builds the app, deploys to Clever Cloud via their CLI or git remote. Configuration lives in `app/.clevercloud/`.

---

## 12. The cookbooks for June 4

Ten cookbooks must be ready by **June 4, 2026** for the June 9 keynote. Cookbooks #1–#3 establish the base agent, private/domain knowledge, and real-time data. Cookbook #4 strengthens the agent with LangChain/LangGraph orchestration. Cookbooks #5 and #6 split memory into short-term thread memory and long-term Postgres-backed user memory. Cookbooks #7–#10 then cover LangSmith observability, LangChain guardrails, MCP-powered Stripe actions, and Snowglobe simulation.

### 12.1 Cookbook #1 — "Your First Agent on Nebius"

**Story.** A developer who has only ever called OpenAI from Python clones this repo, swaps in a Nebius key, and ships a production-shape FastAPI agent in under ten minutes.

**What's in scope:**
- Single LLM call to Nebius AgentKit via the OpenAI SDK pointed at Nebius
- Streaming SSE to the client with status and token events
- Pydantic Settings with validation at boot
- structlog JSON logging with request IDs
- slowapi rate limiting on `/agent/run`
- Prometheus metrics on `/metrics`
- Healthcheck endpoints
- CORS and security headers
- Graceful shutdown
- Dockerfile (multi-stage, slim final image)
- One happy-path pytest test
- One streaming SSE test

**What's NOT in scope:**
- LangChain (introduced in a later cookbook)
- Multi-step agents
- External tools (no Tavily, no retrieval)
- Auth (mentioned in "Going further")
- OpenTelemetry tracing (mentioned in "Going further")

**Recommended model:** `meta-llama/Llama-3.3-70B-Instruct` (verify the exact model ID via Nebius docs at integration time).

**Story arc in the README:**
1. The problem: "I called OpenAI from a Python script. How do I get from there to something I'd actually deploy?"
2. The structure: walk through `app/` directory, explain each file
3. The Nebius integration: one paragraph showing the SDK swap is one line
4. The observability: show a sample log line, a sample Prometheus scrape
5. The streaming: show a curl example with SSE output
6. The deployment: Dockerfile + Nebius Compute one-liner
7. Going further: links to cookbook #2 and beyond

### 12.2 Cookbook #2 — "Domain Knowledge with Pinecone Nexus"

**Story.** The agent from cookbook #1 is fluent but ignorant about private or domain-specific data. This cookbook introduces Pinecone Nexus as the knowledge layer, with standard Pinecone retrieval as the runnable fallback while Nexus availability is evolving.

**What's in scope:**
- Everything from cookbook #1 (duplicated, not imported)
- Pinecone-backed retrieval over a curated Goodreads corpus
- Nebius embeddings for ingestion/query embedding
- Grounded answers with citations and confidence metadata
- A Nexus-oriented architecture explained alongside the runnable Pinecone fallback
- Tests with `respx`/mocked clients; no network calls by default

**What's NOT in scope:**
- Real-time web data (Tavily is introduced in cookbook #3)
- Multi-actor graph (LangGraph is introduced in a later cookbook)
- Critic node, evaluation, simulation (introduced in later cookbooks)
- HITL interrupts

**Recommended models:**
- Answer model: `meta-llama/Llama-3.3-70B-Instruct` (verify the exact model ID via Nebius docs)
- Embedding model: `Qwen/Qwen3-Embedding-8B` (verify the exact model ID via Nebius docs)

**Story arc in the README:**
1. The problem: a fluent model is not a domain expert.
2. The target architecture: Nexus as a knowledge engine rather than a plain vector store.
3. The runnable path: Pinecone retrieval over a cookbook-local corpus.
4. The Nebius integration: embeddings plus answer model.
5. The citations: how every answer stays tied to retrieved evidence.
6. Going further: link to Tavily for fresh context.

### 12.3 Cookbook #3 — "Real-Time Data with Tavily"

**Story.** The retrieval-backed agent has strong domain knowledge, but it still needs fresh web context for questions about current companies, people, products, and events.

**What's in scope:**
- Tavily search and extraction for fresh web context
- Model routing between query planning and final synthesis
- Citation-preserving answers over retrieved sources
- Cost and latency visibility for the search/synthesis path

**What's NOT in scope:**
- LangChain/LangGraph orchestration
- Persistent user memory
- Guardrails or adversarial simulation

### 12.4 Cookbook #4 — "Stronger Agents with LangChain and LangGraph"

**Story.** The earlier cookbooks prove the integration path, but the agent logic is still mostly hand-wired. This cookbook introduces LangChain/LangGraph as the point where the agent graduates from a linear request handler to an explicit graph with state and streaming.

**What's in scope:**
- LangChain model integration pointed at Nebius-compatible chat models
- A LangGraph state graph with named nodes for planning, retrieval/tool use, and response writing
- Streaming graph events mapped back to the cookbook SSE event contract
- Explicit state transitions that make later memory, guardrails, and simulation steps easier to attach

**What's NOT in scope:**
- Persistent context or memory primitives; those belong to cookbook #5
- Guardrails validation and re-ask loops
- Snowglobe simulation or evaluation
- HITL interrupts unless they are only mentioned as a future extension

**Implementation note.** Keep cookbook #4 focused on orchestration: graph state, named nodes, and event streaming. Do not introduce persistent context or memory primitives in this cookbook.

### 12.5 Cookbook #5 — "Short-Term Memory with LangChain"

**Story.** After readers understand orchestration, this cookbook introduces thread-level memory: the agent can resolve follow-ups inside one conversation without making the client replay the whole transcript.

**What's in scope:**
- `thread_id` on `/agent/run`
- Short-term thread state for recent turns
- Bounded context injection before the Nebius call
- `DELETE /threads/{thread_id}` for local reset
- Clear explanation that this local memory is not durable

**What's NOT in scope:**
- Durable user memory
- Postgres or other production stores
- Guardrails validation
- Snowglobe simulation

### 12.6 Cookbook #6 — "Long-Term Memory with LangChain and Postgres"

**Story.** Thread memory helps within one conversation, but production agents also need durable user/application memory that survives restarts and new sessions.

**What's in scope:**
- Postgres-backed LangGraph stores following LangChain long-term memory docs
- Demo `user_id` namespacing, with production guidance to derive it from auth
- Bounded recall before model generation
- `GET /memory/{user_id}` and `DELETE /memory/{user_id}`
- Privacy, deletion, staleness, and conflict guidance

**What's NOT in scope:**
- Guardrails validation
- LangSmith observability beyond basic logs/metrics
- Snowglobe simulation

### 12.7 Cookbook #7 — "Observability with LangSmith"

**Story.** A stateful agent can work locally while still being opaque in production. This cookbook makes traces, feedback, and monitoring first-class.

**What's in scope:**
- LangSmith tracing for agent runs
- Tags and metadata for cookbook, model, route, environment, thread, and user context
- Feedback capture for a run
- Privacy controls and SaaS rollout guidance
- Prometheus metrics remain in place

**What's NOT in scope:**
- Guardrails policy enforcement
- Simulation or regression scoring
- External side-effect tools

### 12.8 Cookbook #8 — "Adding Guardrails with LangChain"

**Story.** Once the agent has memory and observability, the next production problem is controlling what it accepts and emits.

**What's in scope:**
- LangChain guardrail middleware
- Input validation for prompt injection, PII, and topic boundaries
- Output validation for schema, safety, and groundedness
- Fail-closed behavior and observable validator metrics

**What's NOT in scope:**
- Adding new memory behavior
- Simulation at scale
- External commerce actions

### 12.9 Cookbook #9 — "Making Actions with MCP and Stripe"

**Story.** The final runtime capability shows how a book agent can safely take an external action by creating a Stripe test-mode checkout link through MCP.

**What's in scope:**
- Stripe MCP server integration in test mode
- A fictional book checkout action
- Explicit approval before creating a payment link
- SSE action events for pending, approved, rejected, and completed states
- Mocked MCP/Stripe calls in tests

**What's NOT in scope:**
- Real money movement
- Unapproved side effects
- A full ecommerce backend

### 12.10 Cookbook #10 — "Testing Before Production with Snowglobe"

**Story.** The completed agent has memory, observability, guardrails, and external actions. The final cookbook tests that full surface before real users do by generating synthetic scenarios, running conversations, and scoring failures.

**What's in scope:**
- Snowglobe scenario/persona generation
- Batch simulation against the action-capable agent
- Scoring transcripts for regressions, edge-case failures, and action safety
- A repeatable test harness suitable for CI or pre-release review

**What's NOT in scope:**
- Adding new runtime capabilities to the agent
- Using simulation scores as a replacement for human review

---

## 13. Conventions to follow

### 13.1 Python

- Type hints on all function signatures (return types included)
- Pydantic models for all I/O (no raw dicts in route handlers)
- Async everywhere in FastAPI handlers
- Imports in this order: stdlib, third-party, local (Ruff `isort` rule enforces this)
- One module per concern (don't put routes and models in the same file)
- No `print()`. Always `logger.info(...)` via structlog.
- No global state. Use FastAPI `Depends()` for client injection.

### 13.2 TypeScript

- Strict mode, no `any`
- Types derived from `recipe.schema.json` via `packages/recipe-schema/src/types.ts`
- Functional components only
- Tailwind classes inline, but extract to component when repeated 3+ times
- No CSS modules, no styled-components

### 13.3 Markdown

- ATX headings (`#` not underlines)
- Fenced code blocks with language tags always
- Reference-style links when a URL is reused, inline otherwise
- One sentence per line in source (helps git diff). Renders identically.

### 13.4 Commits

- Conventional Commits: `feat(cookbook-01): add streaming SSE`, `fix(app): handle empty recipe list`, etc.
- Scopes: `cookbook-NN`, `app`, `scripts`, `schema`, `readme`, `ci`, `docs`
- One logical change per commit

### 13.5 Branches and PRs

- `main` is always deployable
- Feature branches: `cookbook-NN/description`, `app/description`, `infra/description`
- PRs require all CI green before merge
- Squash merge by default

---

## 14. Workflow recipes

### 14.1 Bootstrap a new cookbook

```bash
bun run new
# Interactive prompts: title, slug, partner, difficulty
# Creates cookbooks/NN-<slug>/ from cookbooks/_template/
# Replaces placeholders
# Prints next steps
```

Then:
1. Edit `cookbooks/NN-<slug>/recipe.json`
2. Run `bun run validate` to confirm it's valid
3. Run `bun run build:readme` to refresh the root README
4. Implement the FastAPI app
5. Add tests
6. Add `Dockerfile`, `Makefile`, `.env.example`
7. Write the cookbook `README.md`
8. Open a PR

### 14.2 Work on a cookbook

```bash
cd cookbooks/01-first-agent-on-nebius
uv sync
cp .env.example .env
# Fill NEBIUS_API_KEY
make dev
# In another terminal:
curl -N -X POST http://localhost:8000/agent/run -d '{"prompt":"Hello"}'
```

### 14.3 Work on the app

```bash
bun install
bun run build:recipes
cd app
bun run dev
```

The app reads the manifest produced by `build:recipes`. Re-run it whenever a recipe changes.

### 14.4 Update the root README

Edit a sub-MD in `README/`, then:

```bash
bun run build:readme
git add README.md README/
git commit -m "docs(readme): update intro"
```

### 14.5 Deploy

The app deploys to Clever Cloud on push to `main`. Cookbooks deploy to Nebius Compute via `make deploy` inside each cookbook directory (the Makefile builds the Docker image and pushes to Nebius Container Registry, then triggers a deployment).

---

## 15. Things that look tempting but are wrong

- **A shared Python "base" package between cookbooks.** Tempting because it would reduce duplication. Forbidden because it would couple cookbooks together and break the "clone one folder and run it" promise.
- **A workspace `pyproject.toml` at the root.** Same reason.
- **Reading recipes at runtime in the Next.js app.** Tempting because it would simplify the build pipeline. Forbidden because it would impose a Node.js runtime requirement on the cookbook site and slow it down.
- **Editing `README.md` directly.** It will be overwritten on the next `bun run build:readme`. Edit `README/*.md` instead.
- **Using `os.getenv(...)` directly in Python code.** Always go through Pydantic Settings.
- **Adding LangChain to cookbook #1 or #2.** They are deliberately framework-free. LangChain is introduced in a later cookbook.
- **Adding mypy.** Ruff is enough for this scope. Avoiding mypy is intentional to reduce friction.
- **Switching to Granian, Litestar, or another non-Uvicorn ASGI server.** Uvicorn is the boring standard. Stick with it.
- **Using Bun as the runtime on Clever Cloud.** Build with Bun, run with Node. Configured in Clever Cloud dashboard.
- **Auto-generating cookbook READMEs.** Cookbook READMEs are hand-written. Only the root README is composed.

---

## 16. Open questions for the human

These are decisions still to be made. An agent encountering one of these should stop and ask.

1. **Exact Nebius model IDs to use.** The model strings above are placeholders. Verify against the current Nebius AgentKit model catalog before hardcoding them in recipes.
2. **Branding assets.** Logo SVG, colors, typography. To be provided by Nebius in week 1.
3. **Clever Cloud build configuration.** Bun-native build vs. fallback to npm install. To be confirmed during kickoff.
4. **Nebius Compute deployment target.** VM vs. serverless 3.5. To be confirmed.
5. **Authors field policy.** Should every recipe credit Clément alone, or also Nebius team members who reviewed? To be confirmed.
6. **Exact Nebius model IDs for newer cookbook steps.** The cookbook arc is now fixed as: first agent, domain/private knowledge, real-time data, LangChain/LangGraph, short-term memory, long-term memory, LangSmith observability, LangChain guardrails, MCP actions, and Snowglobe. The remaining uncertainty is the exact current Nebius model catalog names to pin before launch.

---

## 17. Quick reference

| Task | Command |
|---|---|
| Install JS deps | `bun install` |
| Install Python deps (per cookbook) | `cd cookbooks/<name> && uv sync` |
| Dev the app | `bun run dev` |
| Dev a cookbook | `cd cookbooks/<name> && make dev` |
| Validate all recipe.json | `bun run validate` |
| Rebuild root README | `bun run build:readme` |
| Check README is in sync | `bun run build:readme --check` |
| Rebuild recipes for the app | `bun run build:recipes` |
| Test all cookbooks | `bun run test:cookbooks` |
| Bootstrap a new cookbook | `bun run new` |
| Full build | `bun run build` |

---

## 18. When this file becomes wrong

If the code and this file disagree, **this file is authoritative until updated**. Open a PR that updates `AGENTS.md` alongside the code change. The PR description should explain why the convention changed.

This file is read by every coding agent at the start of every session. Keep it sharp.

---

*Last updated: May 26, 2026 — Clément Sauvage*
